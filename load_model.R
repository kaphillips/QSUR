load_model <- function(path,harmonized_use){
    if (harmonized_use == "all") {
        ## Load all models
    } else if () {
        ## load single model
    } else {
        ## No option given, throw an error
    }
    return (model)
}
